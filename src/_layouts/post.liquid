---
layout: default
---

<h1>{{ page.title }}</h1>

{{ content }}


<p style="margin-top:4rem; text-align:center;">
  /// <strong style="margin-inline: .4em;">{{ page.date | date: "%B %-d, %Y" }}</strong> ///
</p>
