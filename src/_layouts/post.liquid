---
layout: default
---

<h1>{{ page.title }}</h1>

{{ content }}


<p style="margin-top:4rem; opacity:0.5; text-align:center; color:var(--nav-color)">/// {{ page.date | date: "%B %-d, %Y" }} ///</p>
